name: Demo Action

on:
  workflow_dispatch:  # Allows manual triggering

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f2005282@ds.study.iitm.ac.in
        run: echo "Hello, world!"
